import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import 'bootstrap/dist/css/bootstrap.min.css'; // imports bootstrap CSS for aesthetics

const root = ReactDOM.createRoot(document.getElementById('app'));
root.render(<App />);
